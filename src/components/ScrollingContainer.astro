---
const { cardContent } = Astro.props;
---

<ul class="full-width">
  {
    cardContent.map((card) => (
      <li>
        <h3>{card.title}</h3>
        <p>{card.body}</p>
      </li>
    ))
  }
</ul>

<style>
ul {
  grid-column: 1 / -1;
  gap: var(--card-gap, 2rem); 
  
  /* scroll-snap-type: inline mandatory; */

  display: flex;
  overflow-x: auto;

  padding-inline: max(20px, 50% - var(--content-width) / 2);

  list-style: none; 
}

li {
  flex-basis: min(90%, 400px);
  flex-shrink: 0;

  /* scroll-snap-align: center; */

  background: var(--card-background);
  color: var(--card-text-color);
  padding: var(--card-padding);
  border-radius: var(--card-border-radius);
 
}

/* ul {
grid-column: 1 / -1;
scroll-snap-type: inline mandatory;

display: flex;
overflow-x: auto;

padding-inline: max(20px, 50% - var(--content-width) / 2);
} 

li {
flex-basis: min(90%, 400px);
flex-shrink: 0;

scroll-snap-align: center;
}


scroll-padding-inline-start: max(20px, 50% - var(--content-width) / 2);
hvis man skriver start i stedet for center, den skal være under padding-inline
*/

 
</style>
